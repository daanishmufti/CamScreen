# CamScreen

## Setup

1. Install dependencies:
   ```
   pip install -r requirements.txt
   ```
2. Run the app:
   ```
   python -m main
   ```

## Features
- Detects and warps a laptop screen from webcam
- Shows ON/OFF state
- Simple, fixed-size OpenCV window

Press ESC to quit.
